import type { GeometryComponentOptions } from "../types.js";

/** Geometry component */
export default (options?: GeometryComponentOptions) => ({
  // positions
  // normals
  // uvs
  // uvs1
  // vertexColors
  // cells
  // weights
  // joints
  // offsets
  // rotations
  // scales
  // colors
  // count
  // multiDraw
  // primitive
  // culled
  ...options,
});
